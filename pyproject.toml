[project]
name = "bili-live-notification"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dataset>=1.6.2",
    "httpx>=0.28.1",
    "python-telegram-bot>=22.5",
]
